Client scaffold for Observarium.

To run locally (requires Node.js >=18 and npm):

```bash
cd client
npm install
npm run dev
```

The app provides a minimal Svelte setup and a theme toggle storing preference
in `localStorage` under `observarium:theme`.
